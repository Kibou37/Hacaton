# Инструкция по внедрению логики в Tilda

## Что это такое?

Это чистая JavaScript логика для определения типа фигуры и рекомендаций по одежде. Она работает без React, TypeScript и других зависимостей - просто чистый JavaScript, который можно использовать на любом сайте, включая Tilda.

## Что делает эта логика?

1. **Анализирует фигуру** на основе параметров пользователя (рост, замеры, цвет волос/глаз/кожи)
2. **Определяет тип фигуры** (песочные часы, прямоугольник, треугольник и т.д.)
3. **Определяет размер** (XS, S, M, L, XL, XXL)
4. **Определяет цветотип** (весна, лето, осень, зима)
5. **Генерирует рекомендации** по одежде, силуэтам, цветам и советам

## Как внедрить в Tilda?

### Шаг 1: Загрузите файл на сервер

1. Загрузите файл `fashion-logic.js` на ваш хостинг или в облачное хранилище (например, GitHub Pages, Dropbox, Google Drive с публичным доступом)
2. Получите прямую ссылку на файл (например: `https://ваш-сайт.com/fashion-logic.js`)

**Альтернатива:** Вы можете вставить код прямо в Tilda (см. Шаг 2, вариант 2)

### Шаг 2: Добавьте JavaScript код на страницу Tilda

#### Вариант 1: Подключение внешнего файла

1. В редакторе Tilda откройте настройки страницы (шестеренка в правом верхнем углу)
2. Перейдите в раздел **"HTML-код"** → **"В конец <body>"**
3. Вставьте следующий код:

```html
<script src="https://ваш-сайт.com/fashion-logic.js"></script>
```

(Замените `https://ваш-сайт.com/fashion-logic.js` на вашу реальную ссылку)

#### Вариант 2: Вставка кода напрямую

1. В редакторе Tilda откройте настройки страницы
2. Перейдите в раздел **"HTML-код"** → **"В конец <body>"**
3. Откройте файл `fashion-logic.js` в текстовом редакторе
4. Скопируйте весь код и вставьте между тегами `<script>` и `</script>`:

```html
<script>
// Весь код из fashion-logic.js здесь
</script>
```

### Шаг 3: Создайте форму в Tilda

1. Добавьте блок **"Форма"** на страницу
2. Создайте поля формы с такими именами (name):
   - `gender` - радиокнопки или селект (значения: `male` или `female`)
   - `height` - число (рост в см)
   - `hairColor` - селект (значения: `blonde`, `brown`, `black`, `red`, `gray`)
   - `eyeColor` - селект (значения: `blue`, `green`, `brown`, `gray`, `hazel`)
   - `skinTone` - селект (значения: `light`, `medium`, `olive`, `tan`, `dark`)
   - `chest` - число (обхват груди в см)
   - `waist` - число (обхват талии в см)
   - `hips` - число (обхват бедер в см)

**Важно:** Имена полей (атрибут `name`) должны точно совпадать с указанными выше!

### Шаг 4: Добавьте обработчик формы

1. В настройках формы Tilda найдите раздел **"Действие при отправке"**
2. Выберите **"JavaScript"** или **"Кастомный код"**
3. Вставьте следующий код:

```javascript
// Предотвращаем стандартную отправку формы
event.preventDefault();

// Собираем данные из формы
const formData = {
  gender: document.querySelector('[name="gender"]:checked')?.value || document.querySelector('[name="gender"]')?.value,
  height: parseInt(document.querySelector('[name="height"]')?.value) || 0,
  hairColor: document.querySelector('[name="hairColor"]')?.value,
  eyeColor: document.querySelector('[name="eyeColor"]')?.value,
  skinTone: document.querySelector('[name="skinTone"]')?.value,
  chest: parseInt(document.querySelector('[name="chest"]')?.value) || 0,
  waist: parseInt(document.querySelector('[name="waist"]')?.value) || 0,
  hips: parseInt(document.querySelector('[name="hips"]')?.value) || 0
};

// Проверяем, что все поля заполнены
if (!formData.gender || !formData.height || !formData.hairColor || 
    !formData.eyeColor || !formData.skinTone || !formData.chest || 
    !formData.waist || !formData.hips) {
  alert('Пожалуйста, заполните все поля!');
  return;
}

// Вызываем функцию анализа
try {
  const result = getFashionRecommendations(formData);
  
  // Теперь у вас есть результат в переменной result
  // result.bodyAnalysis - анализ фигуры
  // result.clothing - рекомендации по одежде
  
  // Пример: показываем результат в alert (замените на ваше отображение)
  alert('Тип фигуры: ' + result.bodyAnalysis.bodyType + '\n' +
        'Размер: ' + result.bodyAnalysis.bodySize + '\n' +
        'Цветотип: ' + result.bodyAnalysis.colorType);
  
  // Здесь вы можете отобразить результат на странице
  // Например, заполнить скрытые блоки Tilda или показать модальное окно
  
} catch (error) {
  alert('Ошибка: ' + error.message);
}
```

### Шаг 5: Отображение результатов

Вам нужно решить, как показывать результаты пользователю. Вот несколько вариантов:

#### Вариант 1: Скрытые блоки Tilda

1. Создайте блоки с результатами (они будут скрыты по умолчанию)
2. В коде обработчика формы добавьте отображение блоков:

```javascript
// Показываем блок с результатами
document.getElementById('results-block').style.display = 'block';

// Заполняем данные
document.getElementById('body-type').textContent = result.bodyAnalysis.bodyType;
document.getElementById('body-size').textContent = result.bodyAnalysis.bodySize;
document.getElementById('color-type').textContent = result.bodyAnalysis.colorType;
// и т.д.
```

#### Вариант 2: Модальное окно Tilda

1. Используйте встроенное модальное окно Tilda
2. Заполните его данными из результата через JavaScript

#### Вариант 3: Новая страница

1. Передайте данные через URL параметры или localStorage
2. На новой странице Tilda отобразите результаты

### Пример полного кода обработчика формы

```javascript
event.preventDefault();

// Собираем данные
const formData = {
  gender: document.querySelector('[name="gender"]:checked')?.value || document.querySelector('[name="gender"]')?.value,
  height: parseInt(document.querySelector('[name="height"]')?.value) || 0,
  hairColor: document.querySelector('[name="hairColor"]')?.value,
  eyeColor: document.querySelector('[name="eyeColor"]')?.value,
  skinTone: document.querySelector('[name="skinTone"]')?.value,
  chest: parseInt(document.querySelector('[name="chest"]')?.value) || 0,
  waist: parseInt(document.querySelector('[name="waist"]')?.value) || 0,
  hips: parseInt(document.querySelector('[name="hips"]')?.value) || 0
};

// Валидация
if (!formData.gender || !formData.height || !formData.hairColor || 
    !formData.eyeColor || !formData.skinTone || !formData.chest || 
    !formData.waist || !formData.hips) {
  alert('Пожалуйста, заполните все поля!');
  return;
}

// Получаем рекомендации
const result = getFashionRecommendations(formData);

// Сохраняем результат в localStorage для использования на другой странице
localStorage.setItem('fashionResult', JSON.stringify(result));

// Или отображаем сразу на странице
displayResults(result);

function displayResults(result) {
  const analysis = result.bodyAnalysis;
  const clothing = result.clothing;
  
  // Пример отображения (адаптируйте под вашу структуру)
  const html = `
    <div class="results">
      <h2>Анализ вашей фигуры</h2>
      <p><strong>Тип фигуры:</strong> ${analysis.bodyType}</p>
      <p><strong>Размер:</strong> ${analysis.bodySize}</p>
      <p><strong>Цветотип:</strong> ${analysis.colorType}</p>
      <p>${analysis.description}</p>
      
      <h3>Рекомендации по одежде</h3>
      <ul>
        ${clothing.clothingTypes.map(type => `<li>${type}</li>`).join('')}
      </ul>
      
      <h3>Советы</h3>
      <ul>
        ${clothing.tips.map(tip => `<li>${tip}</li>`).join('')}
      </ul>
    </div>
  `;
  
  // Вставляем результат в нужный блок
  const resultsContainer = document.getElementById('results-container');
  if (resultsContainer) {
    resultsContainer.innerHTML = html;
    resultsContainer.style.display = 'block';
    
    // Прокручиваем к результатам
    resultsContainer.scrollIntoView({ behavior: 'smooth' });
  }
}
```

## Структура результата

После вызова `getFashionRecommendations()` вы получите объект со следующей структурой:

```javascript
{
  bodyAnalysis: {
    bodyType: 'hourglass' | 'rectangle' | 'triangle' | 'inverted-triangle' | 'apple',
    bodySize: 'xs' | 's' | 'm' | 'l' | 'xl' | 'xxl',
    colorType: 'spring' | 'summer' | 'autumn' | 'winter',
    description: 'Текстовое описание типа фигуры',
    gender: 'male' | 'female'
  },
  clothing: {
    clothingTypes: ['Тип одежды 1', 'Тип одежды 2', ...],
    silhouettes: ['Силуэт 1', 'Силуэт 2', ...],
    shoulderLength: 'Описание длины рукавов',
    waistLength: 'Описание посадки по талии',
    colorPalette: ['#FFE5B4', '#FFD700', ...], // Массив цветов в HEX
    tips: ['Совет 1', 'Совет 2', ...]
  }
}
```

## Полезные советы

1. **Тестирование:** Сначала протестируйте на простой HTML странице, чтобы убедиться, что всё работает
2. **Имена полей:** Убедитесь, что имена полей в форме точно совпадают с теми, что используются в коде
3. **Обработка ошибок:** Добавьте проверки на случай, если пользователь введет некорректные данные
4. **Стилизация:** Используйте CSS блоки Tilda для красивого отображения результатов
5. **Мобильная версия:** Убедитесь, что форма и результаты хорошо выглядят на мобильных устройствах

## Нужна помощь?

Если что-то не работает:
1. Откройте консоль браузера (F12) и проверьте ошибки
2. Убедитесь, что файл `fashion-logic.js` загружен (проверьте вкладку Network)
3. Проверьте, что функция `getFashionRecommendations` доступна (введите в консоли `typeof getFashionRecommendations`)
4. Убедитесь, что имена полей формы совпадают с теми, что в коде

Удачи с внедрением! 🚀

